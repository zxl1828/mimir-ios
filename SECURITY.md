# 安全策略

## 报告问题

发现安全漏洞时请**不要**开公开 Issue，改用 GitHub 的
[私密漏洞报告](https://github.com/zxl1828/deepseek-ios/security/advisories/new)，
或通过仓库所有者主页上的联系方式私下告知。

报告里尽量说明：影响范围、复现步骤、可能后果，以及（如有）修复建议。

## 重点关注

- API Key / MCP Token 泄露（写入日志、`UserDefaults`、崩溃报告或导出文件）
- 标记为「仅本地使用」的记忆片段仍然被发送到云端
- 未获授权的 MCP 服务器收到数据（绕过逐服务器授权）
- 提示注入导致工具越权：MCP 工具、Spotlight、OCR 被模型滥用
- 流式响应解析导致的崩溃、内存耗尽或无限循环

## 安全设计要点

- API Key 与 MCP Token 只存 Keychain
- 语音识别/合成、向量嵌入与检索、OCR、条码识别全部端侧完成，不出设备
- MCP 数据传输按服务器逐项授权，默认不共享
- 记忆条目可标记「仅本地使用」，标记后不进入请求上下文

## 支持范围

只维护 `main` 分支上的最新代码。
